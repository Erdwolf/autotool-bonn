module Code.Type 

( module Code.Type
, module Data.FiniteMap
, module Autolib.Sets
)

where

--  $Id$

import Data.FiniteMap

import Data.Typeable
import Autolib.TypeableFM

import Autolib.Sets
import Autolib.ToDoc

-- | FIXME: das ist unzusammenhängend

data Coder a b =
     Coder { nametag :: Doc
	   , encode :: [a] -> b
	   , decode :: b -> Maybe [a]
	   , decode_hint :: b -> [a]
	   }
     deriving ( Typeable )

type Frequency a = FiniteMap a Int

type Code a b = FiniteMap a [ b ]




